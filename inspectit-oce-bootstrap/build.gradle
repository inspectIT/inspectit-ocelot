dependencies {
   //No bootstrap dependencies so far...
}

jar {
    archivesBaseName = "${project.name}"

    //include all "implementation" dependencies in fat jar
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}