package rocks.inspectit.ocelot.configschema;

/**
 * Defines the type for a {@link ConfigurationPropertyDescription}.
 */
public enum ConfigurationPropertyType {
    COMPOSITE, STRING, INTEGER, FLOAT, BOOLEAN, DURATION, ENUM
}
