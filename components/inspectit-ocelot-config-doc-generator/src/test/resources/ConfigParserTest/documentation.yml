inspectit:
  instrumentation:
    actions:
      a_debug_println:
        doc:
          description: 'Prints a given Object to stdout.'
          input-desc:
            value: Object to be printed
          return-desc: Void
        input:
          value: Object
        is-void: true
        value-body: |
          System.out.println(value);