inspectit:
  instrumentation:
    actions:

      'a_string_concat':
        docs:
          since: '1.2.1'
          description: 'Concatenates two provided inputs to a single String.'
          inputs:
            'a': 'The first input value.'
            'b': 'The second input value.'
          return-value: 'The concatenated inputs as a String.'
        input:
          'a': 'Object'
          'b': 'Object'
        value: 'new StringBuilder().append(a).append(b).toString();'

      'a_string_concat_3':
        docs:
          since: '1.2.1'
          description: 'Concatenates three provided inputs to a single String.'
          inputs:
            'a': 'The first input value.'
            'b': 'The second input value.'
            'c': 'The third input value.'
          return-value: 'The concatenated inputs as a String.'
        input:
          'a': 'Object'
          'b': 'Object'
          'c': 'Object'
        value: 'new StringBuilder().append(a).append(b).append(c).toString();'
