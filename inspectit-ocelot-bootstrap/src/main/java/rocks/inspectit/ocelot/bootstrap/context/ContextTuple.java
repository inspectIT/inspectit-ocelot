package rocks.inspectit.ocelot.bootstrap.context;

/**
 * Used by {@link IContextManager#attachContext(Object)} to provide a undo mechanism for restored contexts.
 */
public interface ContextTuple {
}
